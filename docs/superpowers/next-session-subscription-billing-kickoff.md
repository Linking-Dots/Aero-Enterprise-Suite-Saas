Continue the tenant-page-redesign iteration (priority #6: SUBSCRIPTION / BILLING). Settings (#3), Organization (#4), and Audit/Activity (#5) are DONE, reviewed, and pushed to main. Now redesign the Subscription/Billing cluster.

STANDARD: resource-management canon (the Users page), NOT the settings-shell — these are dashboard / grid / table pages (Overview, Plans grid, Usage meters, Invoices table). Same family as the Audit/Activity work just shipped.

READ FIRST: memory [[tenant-page-redesign-iteration]] (updated this session — Audit/Activity DONE; #6 next) + [[theme-consistency-all-pages]] + [[plan-product-subscriptions]] (CRITICAL: platform billing = plan + product as SEPARATE subscriptions; plan_module pivot DEPRECATED) + [[module-grouping-rule]] + [[nav-menu-navigation]]; the SDD ledger .superpowers/sdd/progress.md (Audit/Activity section + the SaaS HRMAC-catalog finding); the canonical reference packages/aero-ui/resources/js/Pages/Core/Users/Index.jsx; and the just-shipped Audit/Activity work (commits f11ab37fd..1566b71ac) for the 5-tab + nav-collapse + per-tab-HRMAC-gate + live-verify rigor to mirror.

THE CLUSTER (grounded) — SaaS-ONLY:
- Submodule `subscription` in packages/aero-core/config/module.php (~line 136): name "Subscription & Billing", route /subscription, priority 2, `show_in_nav => true`, `plan => 'saas'`. Components: `plans` (actions view/upgrade/downgrade/cancel), `usage` (view), `invoices` (view/download).
- Controller: `Aero\Platform\Http\Controllers\Tenant\TenantSubscriptionController` — lives in aero-platform (NOT aero-core), conditionally registered in aero-core/routes/web.php (~line 236, `if (class_exists(...))` — so it exists ONLY when aero-platform is installed = SaaS mode). Uses Plan / Subscription / UsageRecord models + SubscriptionLifecycleService.
- Pages: packages/aero-ui/resources/js/Pages/Core/Subscription/{Index,Plans,Usage,Invoices}.jsx — ALL four ALREADY import `IndexPageLayout` (partially on-canon, exactly like the old Audit Index was — so this is an AUDIT-AND-FINISH, not a legacy DashboardLayout port).

ROUTES (web.php ~238-243; prefix `subscription`, name `core.subscription.*`; middleware `auth:web` + `resolve.tenant.context`). ⚠️ NOTE the gate uniformity smell (feed the systemic HRMAC audit):
- `core.subscription.index`  GET /              → Inertia Core/Subscription/Index   (gate core.subscription.plans.view) — props subscription/plan/usage/daysLeft
- `core.subscription.plans`  GET /plans         → Inertia Core/Subscription/Plans   (gate plans.view) — props plans/currentPlan/subscription
- `core.subscription.change-plan` POST /change-plan → JSON (gate plans.UPGRADE) — validates plan_id; lifecycleService->upgrade|downgrade; returns JSON {message, subscription}
- `core.subscription.usage`    GET /usage        → Inertia Core/Subscription/Usage  (gate plans.view) — props subscription/plan/usage
- `core.subscription.invoices` GET /invoices     → Inertia Core/Subscription/Invoices (gate plans.view) — props invoices (mapped array)

CURRENT STATE / WHAT NEEDS DOING (grounded):
- All 4 pages use IndexPageLayout but need a canon audit: KPI/Stat strip wired from controller data, theme consistency reaching the table/grid/card surfaces (incl. Plans cards + Usage meters), proper data-shape consumption, skeleton + EmptyState, registered-icons-only, no inline style, Inertia v2 partials.
- change-plan is the one real MUTATION — REAL BILLING (upgrade/downgrade via SubscriptionLifecycleService; subscriptions carry stripe_id). It returns JSON (not an Inertia redirect) — check how Plans.jsx currently invokes it (router.post vs fetch/axios) and standardize: confirm dialog, type=button, single-submit guard, success/error toast, then refresh subscription state. Treat as DB::transaction-backed + immutable-aware on the backend.

OPEN DESIGN QUESTIONS (resolve in brainstorm, don't pre-decide):
1. NAV CONSOLIDATION: the submodule has 3 components (plans/usage/invoices) + the /subscription Index — likely renders as a parent + 3 nav children today. Collapse 3→1 (collapse_nav) and fold into ONE tabbed "Subscription & Billing" page (tabs: Overview / Plans / Usage / Invoices, switched in place via Inertia only:[...] partial reloads on core.subscription.index?tab=) — SAME pattern as the Audit page? Recommend yes for iteration parity; confirm. (If tabbed: extend the controller to a tab-driven index, or keep the 4 routes and drive tabs via them — decide, and don't break the change-plan POST.)
2. INVOICES SEMANTICS (real product call): invoices() currently maps SUBSCRIPTION rows (not real invoices) into an "invoices" shape — i.e., it's subscription history, not a billing-document list. Is that intended, or should it read a real invoices/payments source? Watch [[plan-product-subscriptions]] — billing spans plan + product subscriptions SEPARATELY; the current page only shows the single plan Subscription. Decide scope: presentation-only redesign vs. correcting the data source.
3. PRODUCT vs PLAN dimension: per [[plan-product-subscriptions]], a tenant has a plan subscription AND product subscriptions as separate records. The controller only surfaces the plan Subscription. Does the redesigned cluster need to show product subscriptions too, or is that out of scope for the UI pass (track for later)?

GOTCHAS:
- DUAL-MODE: SaaS-only. Standalone (aeos365-standalone) has NO subscription routes/pages/controller (class_exists gate). The redesign must NOT assume the routes exist in standalone, and must not break a standalone boot. Live-verify on SaaS democorp ONLY.
- Backend edits land in aero-platform (TenantSubscriptionController / SubscriptionLifecycleService), not aero-core — package-first still holds; pages stay in aero-ui (Core/Subscription/*).
- HRMAC gate bugs to flag for the systemic audit (don't necessarily fix here unless trivial): usage/invoices gate on plans.view (not their own usage.view/invoices.view); change-plan DOWNGRADE is gated as plans.upgrade; the `cancel` action exists in config with no route.
- HRMAC nodes: the SaaS central catalog was first-ever seeded THIS session (the aero:sync-module guard fix). subscription is plan='saas' + filtered by ModuleSyncFilterInterface to SUBSCRIBED products — democorp must be subscribed for the nav to appear. Verify the nav link shows for democorp; if not, check the product/plan subscription + re-run tenant-scope sync. PLATFORM-scope sync is still un-run (carryover) — not needed for tenant pages but logged.
- @aero/ui only; no inline style={}; single centralized <style> if any; Inertia v2 (router.* + only:[...], never navigate for tabs); registered icons only (icons.jsx ~48-key registry — NO kebab heroicon strings; the only heroicon allowed is the imported EllipsisHorizontalIcon React component for row-action Menu triggers); money/decimal + currency formatting; status/billing_cycle badges; trial daysLeft handling.

VERIFY LIVE (Playwright): vite is already up (https://aeos365.test:5173, public/hot present). Login democorp.aeos365.test (admin@democorp.com / Aeos365!Admin). Exercise the real nav link + every tab/page: 0 console errors/warnings; tabs switch in place; KPI reflects controller data; change-plan fires exactly ONE request, confirm-guarded, no double-submit; Theme Studio card-style+accent reach every surface incl. Plans cards + Invoices table; confirm nav childCount/link-count via the authenticated #app data-page prop (JSON.parse(document.getElementById('app').dataset.page).props.navigation). NOTE: HRMAC reads in tinker/CLI under SaaS must wrap in AeroMode::withoutTenantContextGuard() (lesson from this session).

THEN: superpowers:requesting-code-review over the branch diff, fix Critical/Important, update memory [[tenant-page-redesign-iteration]] (Subscription/Billing DONE; queue → remaining list pages → systemic {tenant}/HRMAC-gate-uniformity audit), update .superpowers/sdd/progress.md, offer finishing-a-development-branch options.

Work on main in place (vendor/aero junctions — no worktree). Address me as Boss; show tokens burned per reply. Use the SDD/superpowers workflow (brainstorm → plan → execute). Do NOT push unless I say so.
